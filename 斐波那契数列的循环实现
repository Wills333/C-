#include <stdio.h>//循环实现斐波那契数列
int main(){
    int n;
    int a[40];
    scanf("%d",&n);
    a[0]=a[1]=1;
    if(n>=3){
    for (int i = 2; i <= n-1; i++)
        a[i]=a[i-1]+a[i-2];
        printf("%d",a[n-1]);
}
    else printf("%d",a[n-1]);
    return 0;
}
