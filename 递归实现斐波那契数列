#include <stdio.h>//递归实现斐波那契数列
int fib(int n);
int main(){
int n;
scanf("%d",&n);
printf("%d",fib(n));
    return 0;
}
int fib(int n){
    int sum=0;
    if(n<3)
        return 1;
    else
    return fib(n-1)+fib(n-2);
}
