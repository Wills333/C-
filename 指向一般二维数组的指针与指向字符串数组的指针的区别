例如：int (*p)[2]={{1，2}，{3，4}} 等同于 int s[][2]={{1，2}，{3，4}}    二维数组s中第一个[]里内容可有可无，计算机会自动计算。
在声明时，需要标明指向二维数组的内层数组容纳量。
const char *p[2]={"abc","bcd"} 等同于 char c[2][20]={"abc","bcd"}    ps:字符串数组c中的第二个[]里储存着每个字符串的长度。该指针只记录每个字符串数组的地址。
在声明时，标明的是有几个字符串数组。
